fn pow(base: i32, pow: i32) -> i64 {
    
    let a = 2 + 2

    if base == 0 {
        return 0
    } else if (pow == 0) {
        return 1
    }
    
    let res: i64 = base, i : i32
    
    for i = 0; i < pow; ++i {
        res *= base
    }

    return res
}
