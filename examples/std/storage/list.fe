import "../groups/numeric" as numeric

struct List<T> {
    let mut buf: &T
    let mut size: u64
    let mut capacity: u64

    fn new() -> Self {
        let item: T
        let list: Self
        // list.buf = calloc(1, #sizeof(item))
        list.size = 0
        list.capacity = 1

        return list
    }

    fn reserve<U>(self: &mut Self, additional: U) -> void where numeric::Unsigned<U> {
        let item: T
        self.capacity = self.size + additional to u64
        // self.buf = realloc(self.buf, #sizeof(item) * self.capacity)
    }

    fn push(self: &mut Self, item: T) -> void {
        if self.size >= self.capacity {
            self.reserve(self.size)
        }

        self.buf[self.size++] = item
    }

    fn at<U>(self: Self, index: U) -> T where numeric::Unsigned<U> {
        self.buf[index]
    }
    
    fn pop(self: &mut Self) -> T {
        // panic if self.size is 0
        self.buf[--self.size]
    }
}


