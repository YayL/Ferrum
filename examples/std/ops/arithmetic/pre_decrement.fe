import "../../groups/numeric" as numeric
import "addition" as addition

trait Pre_Decrement {
	fn inline #OP_PRE_DECREMENT(ref: &mut Self) -> mut Self
}

impl Pre_Decrement for (impl addition::Addition) {
	fn inline #OP_PRE_DECREMENT(ref: &mut Self) -> mut Self {
        *ref = *ref - 1
	}
}

