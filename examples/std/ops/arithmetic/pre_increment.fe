import "../../groups/numeric" as numeric
import "addition" as addition

trait Pre_Increment {
	fn inline #OP_PRE_INCREMENT(ref: &mut Self) -> Self
}

impl Pre_Increment for (impl addition::Addition) {
	fn inline #OP_PRE_INCREMENT(ref: &mut Self) -> Self {
        *ref = *ref + 1
	}
}


