trait Address_Of {
    fn inline #OP_ADDRESS_OF(value: Self) -> &Self
}

impl Address_Of<T> for (mut T) {
    fn inline #OP_ADDRESS_OF(value: mut T) -> &mut T {
        let ref: Self
        #llvm_store("ptr", #llvm_register_of(ref), #llvm_register_of(value))
        ref
    }
}
