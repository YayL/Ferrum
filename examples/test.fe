package "arithmetic"

struct String {
    let buf: &u8
    let length: i32

    fn new() -> Self {
        self.buf = 0
        self.length = 0
    }
}

fn mult(a: i64, b: i64) -> i64 {
    a * b
}

fn main(argc: i32, args: String) -> u8 {
    let b: i32 = 0

    mult(argc + argc, 2)
}

