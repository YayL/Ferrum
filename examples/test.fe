import "std/lib" as std

fn main(argc: i32) -> i32 {
    func_1(argc)
}

fn func_1(foo: i32) -> i32 {
    let a: i64 = (2 + 222) * foo to i64

    let b: i64 = a - foo

    let mut i: i32 = 0

    for i < b, i++ {
        i = i + 1
   }
}
